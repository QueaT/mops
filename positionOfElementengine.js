class PositionOfElement {
    constructor(state) {
        this._state = state
    }

    set initialValue(initial) {
       this.initial = initial
       console.log(this.initial);
    }
    startEngine(){
        if(this.initial.includes(this._state)){
            
        }
    }
}


export default PositionOfElement